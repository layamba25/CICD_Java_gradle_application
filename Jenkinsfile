pipeline{
    agent any 
    environment{
        VERSION = "${env.BUILD_ID}"
    }
    stages{
        stage("Test"){
            }
            steps{
              echo "==========STEP ONE=============="
                    }

                }  
            }
        }

    }


}
